#include <iostream>
#include <map>  // Include map header

int main() {
    std::multimap<int, int> m; // Declare a multimap
    // m.insert({10,20});
    // m.insert({20,24});
    // m.insert({10,23});
    // same key value will be inserted two times in multimap
}